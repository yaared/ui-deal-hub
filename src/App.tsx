import SearchBar from './components/SearchBar'

function App() {
  return (
    <main style={{
      minHeight: '100vh',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      padding: '2rem'
    }}>
      <SearchBar />
    </main>
  )
}

export default App
